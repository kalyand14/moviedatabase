<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".features.movie.presentation.movielist.MovieListActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_movie"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/blue_grey_dark"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Only visible when there are no favorite movies -->
    <TextView
        android:id="@+id/tv_empty"
        style="@style/EmptyTextStyle"
        android:drawablePadding="@dimen/padding_activity"
        android:textAlignment="center"
        android:textColor="@color/color_offline"
        app:layout_constraintBottom_toBottomOf="@+id/rv_movie"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/rv_movie"
        app:layout_constraintTop_toTopOf="@+id/rv_movie" />


    <ProgressBar
        android:id="@+id/pb_list_loading_indicator"
        android:layout_width="@dimen/size_list_loading_indicator"
        android:layout_height="@dimen/size_list_loading_indicator"
        android:layout_gravity="center"
        app:layout_constraintBottom_toBottomOf="@+id/rv_movie"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/rv_movie"
        app:layout_constraintTop_toTopOf="@+id/rv_movie"
        android:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>